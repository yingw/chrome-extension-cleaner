{
  "name": "chrome-extension-cleaner",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check:clippy": "cd src-tauri && cargo clippy --all-targets -- -D warnings",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
